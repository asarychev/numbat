[workspace]
resolver = "3"

members = [
    "numbat",
    "numbat-exchange-rates",
    "numbat-cli", "numbat-jupyter",
]

exclude = [
    "numbat-wasm"
]

[profile.release]
lto = true
strip = true
codegen-units = 1
